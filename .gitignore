# Gradle
.gradle/
build/
out/

# IntelliJ Platform Gradle plugin scratch dir (downloaded IDE artifacts, sandbox state, etc.)
.intellijPlatform/

# Kotlin
.kotlin/

# IntelliJ / Android Studio
.idea/*
!.idea/codeStyles/
!.idea/detekt.xml
!.idea/externalDependencies.xml
!.idea/inspectionProfiles/
!.idea/runConfigurations/
!.idea/ktfmt.xml
!.idea/vcs.xml
*.iml

# OS
.DS_Store

# Worktrees
/.worktrees/
/worktrees/
/.claude/worktrees/

# Plans / scratch
.plans/
/.agents/tasks/
.agents/skills/*/tmp/
/.compose-pi-workspace.lock

# Temp
.tmp/

# MkDocs build output
/site/

# SwiftPM (recording/native/macos)
recording/native/macos/.build/
recording/native/macos/.swiftpm/
recording/native/macos/Package.resolved
/.agents/skills/spectre-workspace/

# Cargo (recording/native/linux). Cargo.lock is left untracked deliberately — the helper is
# bundled into the recording jar at build time and the build is reproducible enough from
# Cargo.toml alone given the `rust-version = "1.75"` floor.
recording/native/linux/target/
recording/native/linux/Cargo.lock
